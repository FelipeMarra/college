/*
    O cabeçalho (header) da biblioteca:
    Nele são declaradas o que estará presente na biblioteca, todas suas funçoes e variáveis
*/
 //São declaradas funções que retornarão um inteiro e tem como parâmetro duas variáveis (a e b), também do tipo inteiro
int soma (int a, int b);
int subtracao (int a, int b);
int produto (int a, int b);
int divisao (int a, int b);
